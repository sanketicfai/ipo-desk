name: Update IPO data

on:
  schedule:
    - cron: "30 3-16 * * *"   # 03:30-16:30 UTC = 09:00-22:00 IST, hourly
  workflow_dispatch: {}        # manual "Run workflow" button

permissions:
  contents: write

concurrency:
  group: scrape
  cancel-in-progress: false

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install requests beautifulsoup4 python-dateutil

      - name: Scrape IPO data
        env:
          FNA_API_KEY: ${{ secrets.FNA_API_KEY }}
        run: python daily_ipo_update.py

      - name: Commit fresh data
        run: |
          git config user.name "ipo-desk-bot"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add ipo_data.json gmp_session_log.json qib_intraday_log.json fresh_ofs_cache.json
          git diff --cached --quiet || git commit -m "data update $(date -u +'%Y-%m-%d %H:%M UTC')"
          git push
